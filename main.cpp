#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    double d;

    cin >> d;

    cout << setprecision(1) << fixed << round((d*d)/2);
    
    return 0;
}